apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "argocd-diff-preview.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "argocd-diff-preview.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.serviceAccount.annotations | nindent 4 }}
