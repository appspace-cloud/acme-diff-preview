# Dev / CI dependencies only. Runtime deps live in requirements.txt
# (PyYAML + cydifflib + zstandard as of COPS-2631); keep this file for test tooling.
# Pin exact versions to ensure reproducible CI runs, and keep them equal to
# the versions developers actually run locally: a CI pin that lags the local
# toolchain by a major version (pytest 8 vs 9) is the same class of silent
# local-vs-CI divergence that has produced real bugs in this repo before
# (machine-uptime-dependent monotonic timestamps, git init.defaultBranch).
# To update: pip install pytest --upgrade && pip freeze > requirements-dev.txt
pytest==9.1.1
pytest-cov==7.1.0
coverage==7.15.4
PyYAML==6.0.3
cydifflib==1.2.0
zstandard==0.25.0
# Property-based tests (tests/test_property_based.py) run in CI's `test` job.
hypothesis==6.165.3
# Mutation testing (local, opt-in): `mutmut run` — config in pyproject.toml.
mutmut==3.7.0
# COPS-2595: suite speed and safety.
# pytest-xdist  -- parallel workers; run with `-n auto --dist loadfile` so each
#                  file's tests stay on one process (diff_preview has
#                  module-level mutable globals). See TESTING.md.
# pytest-timeout -- per-test cap so a non-hermetic test fails fast and visibly
#                  instead of silently retrying against a real network.
pytest-xdist==3.8.0
pytest-timeout==2.4.0
